# addon id
id=tpmanager@mozillaonline.com

# directory name, same as git name by default.
name=tpmanager

# git url
git=git://github.com/MozillaOnline/tpmanager.git

# extension base dir
# Extension related filed may be placed in an subdir, so need to posint the base dir.
basedir=

# branches or tags needed to be packed, split by comma
branches=master
